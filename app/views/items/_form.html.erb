<%= form_for [@item_category, @item] do |f| %>
  <% if @item.errors.any? %>
    <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>
		<ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>
  <%= f.label :rate %>
  <%= f.text_field :rate %>
  <br><%= f.submit "Submit" %>
<% end %>
<br><%= link_to 'Back', item_category_items_path(@item_category) %>